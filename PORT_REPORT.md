# Port report — `ed25519-dalek-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `ed25519-dalek`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 7 `use` sites from 1 other repos
- **Outbound**: 32 `use` sites to 4 other repos
- **Unresolved external**: 10 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `ed25519-dalek` | `ed25519_dalek` | 7 | 4 | COD-47 | 146 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (7 edges, 1 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-agent-identity` | 7 | ✅ |

### Symbol-level demand (7 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `ed25519_dalek::Signature` | 1 | 1 | 0 | 1 |
| `ed25519_dalek::Signer` | 1 | 1 | 0 | 1 |
| `ed25519_dalek::SigningKey` | 1 | 1 | 0 | 1 |
| `ed25519_dalek::Verifier` | 1 | 1 | 0 | 1 |
| `ed25519_dalek::VerifyingKey` | 1 | 1 | 0 | 1 |
| `ed25519_dalek::pkcs8::DecodePrivateKey` | 1 | 1 | 0 | 1 |
| `ed25519_dalek::pkcs8::EncodePrivateKey` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 7 symbols)

##### `ed25519_dalek::Signature` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:405` — `use ed25519_dalek::Signature`

##### `ed25519_dalek::Signer` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:14` — `use ed25519_dalek::Signer as _`

##### `ed25519_dalek::SigningKey` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:15` — `use ed25519_dalek::SigningKey`

##### `ed25519_dalek::Verifier` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:406` — `use ed25519_dalek::Verifier as _`

##### `ed25519_dalek::VerifyingKey` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:16` — `use ed25519_dalek::VerifyingKey`

##### `ed25519_dalek::pkcs8::DecodePrivateKey` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:17` — `use ed25519_dalek::pkcs8::DecodePrivateKey`

##### `ed25519_dalek::pkcs8::EncodePrivateKey` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-agent-identity` `src/lib.rs:18` — `use ed25519_dalek::pkcs8::EncodePrivateKey`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `serde-kotlin` | 11 | 5 | `serde` | COD-128 | 1 | To Do |
| `rand-kotlin` | 7 | 5 | `rand` | COD-114 | 14 | To Do |
| `zeroize-kotlin` | 4 | 2 | `zeroize` | COD-196 | 15 | To Do |
| `sha2-kotlin` | 10 | 3 | `sha2` | COD-135 | 23 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `criterion` | 385 | 11 | `ed25519-dalek` |
| `curve25519-dalek` | 42 | 13 | `ed25519-dalek` |
| `rand_core` | 40 | 16 | `ed25519-dalek` |
| `hex-literal` | 16 | 1 | `ed25519-dalek` |
| `ed25519` | 16 | 8 | `ed25519-dalek` |
| `subtle` | 13 | 2 | `ed25519-dalek` |
| `x25519-dalek` | 12 | 3 | `ed25519-dalek` |
| `hex` | 7 | 2 | `ed25519-dalek` |
| `signature` | 2 | 2 | `ed25519-dalek` |
| `merlin` | 1 | 1 | `ed25519-dalek` |
