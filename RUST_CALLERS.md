# Rust callers — `ed25519-dalek-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `ed25519-dalek` | `ed25519_dalek` | 7 | 1 | 1 | 7 |

## `ed25519-dalek`

### Inbound symbols (7 distinct, 7 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `ed25519-dalek`.

#### `ed25519_dalek::Signature` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-agent-identity` `src/lib.rs:405` — `use ed25519_dalek::Signature`

#### `ed25519_dalek::Signer` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-agent-identity` `src/lib.rs:14` — `use ed25519_dalek::Signer as _`

#### `ed25519_dalek::SigningKey` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-agent-identity` `src/lib.rs:15` — `use ed25519_dalek::SigningKey`

#### `ed25519_dalek::Verifier` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-agent-identity` `src/lib.rs:406` — `use ed25519_dalek::Verifier as _`

#### `ed25519_dalek::VerifyingKey` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-agent-identity` `src/lib.rs:16` — `use ed25519_dalek::VerifyingKey`

#### `ed25519_dalek::pkcs8::DecodePrivateKey` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-agent-identity` `src/lib.rs:17` — `use ed25519_dalek::pkcs8::DecodePrivateKey`

#### `ed25519_dalek::pkcs8::EncodePrivateKey` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-agent-identity` `src/lib.rs:18` — `use ed25519_dalek::pkcs8::EncodePrivateKey`
