# Port targets — `ed25519-dalek-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `ed25519-dalek` that codex-rs uses, and therefore the minimum subset that `ed25519-dalek-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-47] (rank `146`, tier `direct`, status `To Do`)

**Cargo package name**: `ed25519-dalek`
**Rust module name (in `use` statements)**: `ed25519_dalek`

## Summary

- **7** distinct symbols imported by codex-rs
- **7** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `ed25519_dalek::Signer`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-agent-identity`
- Aliases used: `as _`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `agent-identity/src/lib.rs:14` (`codex-agent-identity`) — `use ed25519_dalek::Signer as _`

### `ed25519_dalek::SigningKey`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-agent-identity`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `agent-identity/src/lib.rs:15` (`codex-agent-identity`) — `use ed25519_dalek::SigningKey`

### `ed25519_dalek::VerifyingKey`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-agent-identity`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `agent-identity/src/lib.rs:16` (`codex-agent-identity`) — `use ed25519_dalek::VerifyingKey`

### `ed25519_dalek::pkcs8::DecodePrivateKey`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-agent-identity`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `agent-identity/src/lib.rs:17` (`codex-agent-identity`) — `use ed25519_dalek::pkcs8::DecodePrivateKey`

### `ed25519_dalek::pkcs8::EncodePrivateKey`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-agent-identity`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `agent-identity/src/lib.rs:18` (`codex-agent-identity`) — `use ed25519_dalek::pkcs8::EncodePrivateKey`

### `ed25519_dalek::Signature`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-agent-identity`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `agent-identity/src/lib.rs:405` (`codex-agent-identity`) — `use ed25519_dalek::Signature`

### `ed25519_dalek::Verifier`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-agent-identity`
- Aliases used: `as _`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `agent-identity/src/lib.rs:406` (`codex-agent-identity`) — `use ed25519_dalek::Verifier as _`
